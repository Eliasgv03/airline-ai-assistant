# ========================================
# Airline AI Assistant - Environment Variables
# ========================================
# Copy this file to .env and fill in your values
# cp .env.example .env

# ========================================
# REQUERIDAS - Debes configurar estas para que funcione
# ========================================

# Base de datos PostgreSQL con pgvector
# Para desarrollo local con Docker:
#   DATABASE_URL=postgresql://postgres:postgres@localhost:5432/airline_ai
# Para producción (Supabase/Render):
#   DATABASE_URL=postgresql://user:password@host:port/database
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/airline_ai

# Proveedor LLM - Elige uno: groq o gemini
# Recomendado: groq (mejor para free tier)
LLM_PROVIDER=groq

# API Key del proveedor LLM elegido
# Si LLM_PROVIDER=groq, necesitas GROQ_API_KEY
# Si LLM_PROVIDER=gemini, necesitas GOOGLE_API_KEY
# Obtén tu API key:
#   Groq: https://console.groq.com/keys
#   Google: https://makersuite.google.com/app/apikey
GROQ_API_KEY=your_groq_api_key_here
# GOOGLE_API_KEY=your_google_api_key_here

# ========================================
# OPCIONALES - Tienen valores por defecto, pero puedes personalizarlos
# ========================================

# Entorno de ejecución
ENVIRONMENT=development
# LOG_LEVEL=info  # Opciones: debug, info, warning, error

# Modelos LLM (opcionales, tienen valores por defecto)
# GEMINI_MODEL=gemini-2.5-flash-lite
# GROQ_MODEL=llama-3.3-70b-versatile

# Vector Store Configuration
# VECTOR_STORE_COLLECTION_NAME=air_india_policies
# EMBEDDING_DIMENSION=384  # IMPORTANTE: Debe ser 384 (no 768)

# ========================================
# OPCIONALES - Funcionalidades Avanzadas
# ========================================

# LangSmith Tracing (para debugging y monitoreo de LLM)
# Deshabilitado por defecto. Solo habilita si necesitas debugging avanzado.
# LANGSMITH_TRACING=false
# LANGSMITH_API_KEY=your_langsmith_api_key
# LANGSMITH_PROJECT=airline-ai-assistant
# LANGSMITH_ENDPOINT=https://api.smith.langchain.com

# Amadeus Flight API (para búsqueda real de vuelos)
# Deshabilitado por defecto. Solo habilita si tienes credenciales de Amadeus.
# USE_REAL_FLIGHT_API=false
# AMADEUS_API_KEY=your_amadeus_api_key
# AMADEUS_API_SECRET=your_amadeus_api_secret
# AMADEUS_USE_TEST=true  # Usar entorno de prueba (recomendado)
# FLIGHT_API_TIMEOUT=5  # Timeout en segundos

# ========================================
# NOTAS IMPORTANTES
# ========================================
# 1. Mínimo requerido: DATABASE_URL, LLM_PROVIDER, y la API key correspondiente
# 2. EMBEDDING_DIMENSION debe ser 384 (el modelo all-MiniLM-L6-v2 usa 384 dimensiones)
# 3. DATABASE_URL: No necesitas especificar el driver (+asyncpg o +psycopg),
#    el código lo maneja automáticamente
# 4. Para producción, estas variables se configuran en el dashboard de Render,
#    no en un archivo .env
# 5. Nunca commitees el archivo .env al repositorio (está en .gitignore)
